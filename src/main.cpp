#include <iostream>
#include <calc.h>
using namespace std;
int main (int argc, char** argcl) {
    while (true){
        int a, b;
        cin >> a >> b;
        cout << plusik(a, b) << endl;
    }
    return 0;
}
